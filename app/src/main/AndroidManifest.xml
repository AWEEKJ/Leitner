<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uos.leitner">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:name=".LeitnerApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustPan">
        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.IntroLoginActivity"></activity>
        <activity android:name=".ui.activity.IntroSigninActivity" />
        <activity android:name=".ui.activity.IntroGoogleSigninActivity"></activity>
        <activity android:name=".ui.activity.CategoryEditActivity" />
        <activity android:name=".ui.activity.IntroActivity" />
        <activity android:name=".ui.activity.CategoryAddActivity"></activity>
        <activity android:name=".ui.activity.SettingListActivity"></activity>

        <activity android:name=".ui.activity.MeasureResultActivity"
            android:theme="@android:style/Theme.DeviceDefault.Light.Dialog"/>

    </application>

</manifest>